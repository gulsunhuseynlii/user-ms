spring:
  application:
    name: user-ms

  mail:
    host: smtp.gmail.com
    port: 587
    username: gulsunhuseynli100@gmail.com
    password: jito vvmf tiec ledc

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  datasource:
    url: jdbc:postgresql://localhost:5432/user_db
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none

  liquibase:
    change-log: classpath:liquibase/db.changelog-master.yaml
    enabled: true

server:
  port: 8082